import { DashboardPage } from '@/features/dashboard/components/DashboardPage';

export const metadata = { title: 'Dashboard CTO — Software Factory Simulator' };

export default function Dashboard() {
  return <DashboardPage />;
}
